#include <stdlib.h>
		//枚举元素
		//第一个元素值默认为0 向下每个元素依次加1
enum Days { Sunday, Monday, Tuesday, Wednesday, Thursday, Friday, Saturday };

int main()
{
	int a;
scanf("%d", &a);

switch(a) {
    case 0:
        printf("xing qi ri ");
        break;
    
}

	
}
